#include "main.h"
/**
 * mul - function for multiplying two numbers.
 * @a: the first number.
 * @b: the second number.
 *
 * Return: the product of the two numbers.
 */
int mul(int a, int b)
{
	return (a * b);
}
